import { component$ } from "@builder.io/qwik";
import { Form } from "@builder.io/qwik-city";
import { Flex } from "~/styled-system/jsx";

export const FormDiscovery = component$(() => {
    return <Flex>
        <Form></Form>
    </Flex>
});